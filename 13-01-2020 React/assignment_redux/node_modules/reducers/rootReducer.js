import data from '../../src/api'

const rootReducer = (state, action) => {
    if (action.type == "CHANGE_DATA" && action.data != null)
        return {data: action.data};
    return {data: data};
};

export default rootReducer;